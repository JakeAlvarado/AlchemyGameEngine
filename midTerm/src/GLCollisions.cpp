#include "GLCollisions.h"

GLCollisions::GLCollisions()
{

}

GLCollisions::GLCollisions(vec2 new_pos, float new_length, float new_width) : pos(new_pos), length(new_length), width(new_width) {}

GLCollisions::~GLCollisions()
{

}
